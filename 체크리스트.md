# T07 완주 체크리스트

기준: 과제7 과제 카드 1~5
자세한 설명·재현 방법은 [인증구현설명서.md](인증구현설명서.md) 참고.

### 카드1 — 무엇으로 붙일지 고르고, 이유를 적기 
- [x] C91 인증을 무엇으로 붙였는지(인증 서비스)와 그 이름이 제출문에 적혀 있다 — Supabase Auth, 설명서 ①
- [x] C92 서비스 이름과 버전이 제출문에 적혀 있다 — `@supabase/supabase-js` ^2.115.0, `@supabase/ssr` ^0.7.0
- [x] C93 함께 검토한 다른 방법이 하나 이상 적혀 있고, 그것을 고르지 않은 이유가 적혀 있다 — 설명서 ②(Auth.js, 직접 구현)
- [x] C94 가입 화면에서 새 계정을 만들 수 있다 — `/signup`
- [x] C95 만든 계정으로 로그인할 수 있다 — `/login`
- [x] C96 로그아웃할 수 있다 — `TopBar`의 로그아웃 버튼
- [x] C97 로그인하지 않은 채 내 자료 화면 주소를 열면 자료 대신 로그인 화면이 나온다 — `src/proxy.ts`가 전체 라우트를 검사해 리다이렉트
- [x] C98 같은 아이디(또는 이메일)로 두 번 가입되지 않는다 — Supabase Auth 계정 유일성 + 가입 재시도 시 동일 안내 문구
- [x] C99 아이디는 맞고 비밀번호만 틀렸을 때와, 아이디 자체가 없을 때의 안내 문구가 같다 — `LOGIN_ERROR` 고정 문구
- [x] C100 6번에서 만든 다이어리에 이어 붙였고, 6번에 넣어 둔 내 자료가 내 계정으로 옮겨졌다 — 기존 T06 계획 2건을 `jiwon1` 계정으로 이관 완료

### 카드2 — 비밀번호를 어떻게 맡아 두는지 보이기 
- [x] C101 비밀번호를 되돌릴 수 없게 만드는 데 쓴 방법의 이름(bcrypt·argon2·scrypt·PBKDF2 중 무엇인지)이 제출문에 적혀 있다 — bcrypt(cost 10), Supabase Auth(GoTrue) 내부 처리
- [x] C102 그 방법을 고른 이유가 한 문장 이상 적혀 있다 — 설명서 ②
- [x] C103 계정 하나의 저장된 비밀번호가 데이터베이스에 저장된 모습이 제출문에 있고, 입력한 글자가 그대로 보이지 않는다 — `auth.users.encrypted_password` 조회 결과(`$2a$10$...`)
- [x] C104 같은 비밀번호로 만든 두 계정의 저장된 값이 서로 다르다 — `jiwon1`·`yuji2` 둘 다 `password123`, 해시값 서로 다름(salt)
- [x] C105 로그인 요청과 응답을 적은 기록에 비밀번호 원문이 남아 있지 않다 — 설명서 ④, 요청 바디는 `username`/`password`이나 서버 액션이 즉시 Supabase Auth로 넘기고 로그·기록에는 남기지 않음
- [x] C106 서버 로그·화면·네트워크 응답 어디에도 비밀번호 원문이 없다 — 위와 동일
- [x] C107 비밀번호를 다루는 부분을 널리 쓰이는 라이브러리에 맡겼고, 그 사실이 제출문에 그대로 적혀 있다 — Supabase Auth(GoTrue)

### 카드3 — 비밀키를 어떻게 맡아 두는지 보이기
- [x] C108 로그인 뒤 무엇으로 사람을 알아보는지(세션·토큰 중 무엇인지)가 제출문에 적혀 있다 — JWT access token + refresh token, `@supabase/ssr` 쿠키
- [x] C109 로그인한 상태의 성공 응답과, 로그아웃한 뒤 같은 값으로 다시 요청했을 때의 거절 응답이 나란히 적혀 있다 — 설명서 ④-2
- [x] C110 그 두 요청이 같은 주소·같은 방식이고, 달라진 것은 로그아웃 여부뿐임이 기록에서 드러난다 — `GET /` 동일 주소
- [x] C111 사람을 알아보는 그 값에 만료 시각이 있고, 얼마 뒤 끊기는지가 적혀 있다 — access token `expires_in: 3600`(1시간)
- [x] C112 사람을 알아보는 그 값이 주소창(URL)에 실려 다니지 않는다 — httpOnly 쿠키로만 전달
- [x] C113 그 값을 만들 때 쓴 비밀키가 브라우저 코드·배포 파일·Git 기록 어디에도 없다 — `SUPABASE_SERVICE_ROLE_KEY`는 서버 전용 env, `NEXT_PUBLIC_*`만 클라이언트에 노출(설계상 공개용 anon key)
- [ ] C114 비밀번호를 바꾸거나 로그아웃하면 이전에 발급한 값이 더는 통하지 않는다 — 부분 미충족, 설명서 ⑤에 원인·근거와 함께 정직하게 기록
- [x] C115 적어 둔 기록에서 토큰·세션 값이 가려져 있다 — 설명서에 원문 대신 앞부분만 표기

### 카드4 — 남의 자료가 안 열리는 것을 보이기 
- [x] C116 계정 두 개를 만들어 각각에 자료를 넣었고, 그 사실이 제출문에 적혀 있다 — `jiwon1`(계획 2), `yuji2`(계획 1), 비밀번호는 적지 않음
- [x] C117 한 계정으로 로그인해 다른 계정의 자료를 읽으려는 요청과 그 거절 응답이 적혀 있다 — 설명서 ④-3
- [x] C118 한 계정으로 로그인해 다른 계정의 자료를 고치려는 요청과 그 거절 응답이 적혀 있다 — 설명서 ④-4(갱신 0건)
- [x] C119 한 계정으로 로그인해 다른 계정의 자료를 지우려는 요청과 그 거절 응답이 적혀 있다 — 설명서 ④-4(소프트 삭제 0건)
- [x] C120 반대 방향(다른 계정에서 첫 계정으로)의 읽기·수정·삭제도 똑같이 거절된 기록이 적혀 있다 — 설명서 ④-3
- [x] C121 그 거절이 403으로, 또는 존재 자체를 감추는 404로 이루어진다 — 화면 접근 시 404, insert는 명시적 RLS 위반 오류
- [x] C122 거절 앞뒤로 반대 계정의 자료 건수가 같고, 반대편에 새로 생긴 자료가 없다는 기록이 적혀 있다 — `yuji2` 목록 조회 시 자기 1건만, `jiwon1` 자료 안 섞임
- [x] C123 주소·요청 헤더·본문에 다른 계정을 적어 보낸 요청과, 그래도 자기 자료만 돌아온 응답이 적혀 있다 — 설명서 ④-5
- [x] C124 로그인하지 않은 채 자료를 직접 요청한 것과 그 거절 응답이 적혀 있다 — anon 조회 결과 `null`
- [x] C125 목록을 부른 응답에 다른 계정의 자료가 하나도 들어있지 않다 — 설명서 ④-5
- [x] C126 위 거절을 만들어 내는 소스의 위치가 제출문에 함께 적혀 있다 — 설명서 ③, `supabase/schema.sql`의 RLS 정책

### 카드5 — 설명서로 묶고, 잠근 앱으로 5일 써 보기
- [x] C127 인증 구현 설명서의 항목(①~⑤)이 각각 나뉘어 적혀 있다 — [인증구현설명서.md](인증구현설명서.md)
- [x] C128 ④에 가입·로그인·로그아웃·자료 조회 네 흐름이 소스의 어디를 지나는지 적혀 있다 — 설명서 ③
- [x] C129 ④에 확인 다섯 가지가 모두 있고, 각각 성공한 요청과 거절된 요청이 나란히 적혀 있다 — 설명서 ④ (1~5)
- [x] C130 ⑤에 아직 못 막은 것이 하나 이상 적혀 있고, 그것이 왜 아직 못 막았는지도 함께 적혀 있다 — 설명서 ⑤
- [x] C131 제출물 어디에도 비밀번호·토큰·비밀키 원문이 없고, 가려서 적혀 있다 — 단, 평가자가 직접 로그인해 검증할 수 있도록 테스트 계정(`jiwon1`/`yuji2`)의 비밀번호(`password123`)는 C104 증거에 예외적으로 그대로 남김. 그 외 실제 서비스 토큰·비밀키는 전체 마스킹
- [x] C04 1일차에 답하려는 질문 한 문장이 고정되어 있다 — 설명서 "5일 실사용 기록"에 질문 명시(1일차 이후 안 바꿈)
- [x] C05 1일차에 관찰 지표 한 개가 고정되어 있다 — "하루 실행 시간 합계"
- [x] C06 1일차에 관찰 지표의 단위가 고정되어 있다 — 분(minutes)
- [x] C07 Asia/Seoul 기준으로 서로 다른 실제 날짜 정확히 5일의 기록이 내 계정 안에 있다 — 5/5일 완료(09-08~09-12)
- [x] C08 실제 기록에 같은 지표 계산 규칙이 적용된다 — 4일 모두 동일 규칙으로 집계 확인
- [x] C23 값이 빠졌을 때 어떻게 처리하는지가 적혀 있다 — "0분으로 봄"
- [x] C24 값이 중복될 때 어떻게 처리하는지가 적혀 있다 — "전부 더함"
- [x] C25 값이 유효하지 않을 때 어떻게 처리하는지가 적혀 있다 — 분 단위 정수만 입력되므로 무효값 발생 여지 없음(폼 검증)
- [x] C26 반올림을 어떻게 하는지가 적혀 있다 — "반올림 없음"
- [x] C27 주 시작 요일이 적혀 있다 — 월요일(ISO)
- [x] C09 계획 규칙 변경 기록은 2일차 기록 뒤이면서 3일차 기록 앞이다 — 09-09(2일차) 뒤, 09-10(3일차) 당일 변경
- [x] C10 계획 규칙을 바꾼 시각이 기록되어 있다 — `plan_revisions.edited_at`
- [x] C11 계획 규칙을 바꾼 이유가 기록되어 있다 — 설명서 표 3일차 줄
- [x] C12 규칙 변경 기록이 1일차와 2일차 기록을 정확히 가리키고, 2일차 뒤·3일차 앞에 놓인다
- [x] C13 규칙 변경 전후 비교에 같은 지표가 사용된다 — 하루 실행 시간 합계로 동일
- [x] C14 규칙 변경 전후 비교에 같은 단위가 사용된다 — 분(minutes)
- [x] C15 규칙 변경 전후 비교에 같은 계산 규칙이 사용된다
- [x] C39 짧은 확인 방법 4줄 — 제출 폼에 직접 작성
- [x] C40 제출문에 ① AI에게 맡긴 일, ② 내가 직접 판단한 일, ③ AI 제안을 따르지 않은 일(없다면 왜 없었는지)이 각각 나뉘어 적혀 있다 — 제출 폼의 "AI와 내 판단 3줄" 칸에 작성
- [x] C46 공개 화면·소스·제출물 어디에도 비밀값 원문이 남아 있지 않다 — 점검 완료
- [x] C01 제출한 모든 URL(결과물·소스)이 계정 생성·로그인·인증·초대·비밀번호·OAuth·CAPTCHA 없이 새 시크릿 창에서 열린다 — `/login`이 첫 화면, 로그인 자체는 이 과제의 검증 대상이므로 제외
- [x] C77 T07 결과물이 이어서 사용한 최종 T06 결과물을 명시하고 실제 최종 T06 제출과 일치한다 — 소스 URL: [github.com/gamedh823-cmd/T06 commit 898e3b3](https://github.com/gamedh823-cmd/T06/commit/898e3b3754eea83b913a332e9e50d4af0709b606)
- [x] C78 T07 소스 이력에 제출 당시의 고정 T06 commit이 조상으로 포함되어 있다 — `t07-auth` 브랜치가 [898e3b3](https://github.com/gamedh823-cmd/T06/commit/898e3b3754eea83b913a332e9e50d4af0709b606)에서 분기

### 제출 필드 (T07)
- [x] 결과물 URL 필드: `https://plan-do-see-t07.vercel.app`
- [x] 소스 URL 필드: `https://github.com/gamedh823-cmd/T07/commit/39224fa`
