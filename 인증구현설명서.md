# T07 인증 구현 설명서

플랜두씨 다이어리(T06)에 가입·로그인을 붙여, 내 계획·할 일·기록이 로그인한 나에게만 보이도록 만들었습니다. 아래 항목은 무엇을 붙였는지, 왜 그걸 골랐는지, 소스의 어디를 고쳤는지, 실제로 안 열리는지 어떻게 확인했는지, 그리고 아직 못 막은 것을 각각 나눠 적습니다. (AI와 내 역할 구분은 제출 폼의 별도 칸에 작성)

## ① 무엇으로 붙였나

**Supabase Auth** (인증 서비스) — `@supabase/supabase-js` ^2.115.0, `@supabase/ssr` ^0.7.0

가입 화면은 이메일이 아니라 **아이디 + 비밀번호**만 받습니다. 화면 뒤에서는 그 아이디를 `아이디@plandosee.app` 형태의 내부용 주소로 바꿔 Supabase Auth에 넘기는데, 이 주소는 실제로 존재하지 않고 메일이 오가지도 않으며 화면 어디에도 노출되지 않습니다 — Supabase Auth가 이메일 필드를 요구하는 구조라서 붙인 얇은 변환층일 뿐, 사용자 입장에서는 처음부터 끝까지 "아이디·비밀번호"입니다.

## ② 왜 그걸 골랐나

이미 같은 Supabase 프로젝트의 Postgres를 데이터 저장소로 쓰고 있어서(`src/lib/supabase/server.ts`), 같은 프로젝트의 인증 서비스를 붙이는 것이 가장 통합 비용이 적었습니다. 비밀번호 해싱과 세션(JWT) 발급·검증을 서비스가 전담하므로, 그 부분에서 실수할 여지가 원천적으로 줄어듭니다.

**함께 검토했지만 고르지 않은 방법**
- **Auth.js(NextAuth) 라이브러리**: 널리 쓰이지만 Credentials(자체 아이디/비번) 방식으로 쓰려면 사용자 테이블·세션 저장소를 별도로 설계해야 해서, 이미 있는 Supabase 스키마와 두 벌로 관리해야 하는 부담이 있어 고르지 않았습니다.
- **직접 구현**: bcrypt로 비밀번호를 직접 해시하고 세션 토큰 발급·검증·만료를 손으로 짜는 방법. 통과 기준(재사용 공격 방지, 세션 만료 등)을 전부 스스로 챙겨야 해서 실수 위험이 가장 크다고 판단해 고르지 않았습니다.

## ③ 어디를 어떻게 고쳤나

**가입·로그인·로그아웃**
- [src/lib/auth-actions.ts](src/lib/auth-actions.ts) — `login`/`signup`/`logout` 서버 액션. 아이디 → 내부용 이메일 변환, 회원가입은 Supabase Admin API(`auth.admin.createUser`, `email_confirm: true`)로 계정을 만들어 바로 로그인 세션을 발급합니다(실제 이메일이 없어 확인 메일을 보낼 수 없으므로).
- [src/app/login/page.tsx](src/app/login/page.tsx), [src/app/signup/page.tsx](src/app/signup/page.tsx) — 아이디·비밀번호 입력 화면.
- [src/components/TopBar.tsx](src/components/TopBar.tsx) — 로그인 상태일 때만 메뉴·로그아웃 버튼 표시.

**자료 조회 — 로그인 여부에 따른 문지기**
- [src/proxy.ts](src/proxy.ts) + [src/lib/supabase/middleware.ts](src/lib/supabase/middleware.ts) — 모든 요청 앞단에서 세션을 검사해, 로그인 안 했으면 `/login`으로 돌려보냅니다(Next.js 16부터 `middleware.ts`가 `proxy.ts`로 이름이 바뀐 것을 반영).
- [src/lib/supabase/server.ts](src/lib/supabase/server.ts) — 요청마다 그 사람의 쿠키(세션)를 실어 만드는 서버용 클라이언트. 이 클라이언트로 나가는 모든 DB 요청이 "누구인지"를 DB(RLS)에 전달합니다.

**자료를 남과 다르게 만들기 — DB 쪽**
- [supabase/schema.sql](supabase/schema.sql), [supabase/migrations/20260908000001_add_auth.sql](supabase/migrations/20260908000001_add_auth.sql) — `plans` 테이블에 `user_id`(계정 참조, 계정 삭제 시 cascade) 추가. 다섯 테이블 모두 "로그인한 사람이 곧 그 자료의 주인"일 때만 열리도록 RLS 정책을 `using(true)`(누구나)에서 `auth.uid() = user_id`(직접 소유) 또는 `plans`까지 조인해 주인을 확인하는 조건으로 교체.
- [src/lib/actions.ts](src/lib/actions.ts) — `createPlan`에 로그인한 사람의 id를 `user_id`로 저장. `updateTodo`/`deleteTodo`는 갱신된 행을 `select()`로 돌려받아, 0건이면(=RLS가 막았으면) "수정할 수 없습니다" 오류를 명시적으로 냅니다.
- [src/lib/queries.ts](src/lib/queries.ts) — 모든 조회 함수가 전역 클라이언트 대신 요청별 인증 클라이언트를 씀 → 조회 자체가 RLS를 통과해야 결과가 나옵니다.
- [src/app/api/export/route.ts](src/app/api/export/route.ts) — 로그인 안 하면 401, 로그인해도 RLS가 본인 자료만 돌려줌.

**계정 삭제(신규)**
- [src/app/api/account/route.ts](src/app/api/account/route.ts) + [src/lib/supabase/admin.ts](src/lib/supabase/admin.ts) — 관리자 API로 "요청한 본인" 계정만 삭제. `user_id`의 `on delete cascade` 덕분에 계정이 지워지면 그 계정의 계획·할 일·기록도 함께 지워집니다.
- [src/app/settings/page.tsx](src/app/settings/page.tsx) — 계정 삭제 화면.

## ④ 안 열리는 것을 확인한 기록

두 계정(`jiwon1`, `yuji2`)을 만들어 서로의 자료로 교차 접근을 시도했습니다. 모두 2026-09-08~09에 실제로 재현했습니다.

**1) 로그인 — 성공 / 실패**
- 성공: `POST /login` (아이디 `jiwon1`, 비밀번호 정확) → 200, 홈 화면으로 리다이렉트, 계획 2건 노출
- 거절: 같은 주소에 존재하지 않는 아이디, 또는 존재하는 아이디+틀린 비밀번호 → 두 경우 모두 동일한 문구("아이디 또는 비밀번호가 올바르지 않습니다") — 아이디 존재 여부가 응답으로 새지 않음

**2) 로그아웃 뒤 같은 주소 재요청 — 성공 / 거절**
- 로그인 상태: `GET /` → 200, 계획 목록 노출
- 로그아웃 후 동일 주소 재요청: `GET /` → 307, `/login`으로 리다이렉트 (같은 주소, 같은 방식, 차이는 로그인 여부뿐)

**3) 계정 간 자료 읽기 — 성공 / 거절 (양방향)**
- 성공: `yuji2`가 자기 계획 조회 → 1건(`유지2의 계획`) 그대로 반환
- 거절: `yuji2`가 `jiwon1`의 계획 id로 직접 조회 → `null` (행 자체가 안 보임)
- 반대 방향(`jiwon1` → `yuji2`의 계획 id)도 동일하게 `null`
- 화면 레벨: `yuji2`로 로그인한 채 `jiwon1` 계획의 URL(`/plans/c63389d9-...`)로 직접 접속 → **404** (페이지 자체가 없는 것처럼 보임 — 자료의 존재를 감춤)

**4) 계정 간 쓰기/삭제 — 거절**
- `yuji2`가 `jiwon1`의 계획 제목을 고치는 요청 → 갱신된 행 0건 (성공한 것처럼 보이지 않고, 실제로 안 바뀜)
- `yuji2`가 `jiwon1`의 할 일을 삭제(소프트 삭제) 시도 → 갱신된 행 0건
- `jiwon1`이 `yuji2`의 계획 id 아래에 할 일을 새로 넣으려는 시도 → 명시적 거절: `new row violates row-level security policy for table "todos"`

**5) 로그인하지 않은 채 직접 요청 / 목록에 남의 자료가 섞이는지**
- 로그인하지 않은 채(anon) `jiwon1`의 계획 id로 직접 조회 → `null`
- `yuji2` 계정으로 전체 계획 목록 조회 → 자기 계획 1건만 나오고, `jiwon1`의 자료는 하나도 섞이지 않음

(AI에게 맡긴 일 / 내가 판단한 일 / AI 제안을 따르지 않은 일 — 제출 폼의 "AI와 내 판단 3줄" 칸에 따로 작성)

## ⑤ 아직 못 막은 것

**로그아웃해도, 로그아웃 전에 이미 발급된 access token 원본을 어딘가에 빼돌려 두었다면 그 토큰은 자연 만료 시각(기본 1시간)까지 계속 통합니다.**

Supabase Auth의 access token은 서버가 상태를 들고 있지 않는(stateless) JWT라서, 로그아웃은 (1) 브라우저 쿠키를 지우고 (2) 다음 토큰을 다시 받아올 수 있는 refresh token을 서버에서 무효화할 뿐, 이미 발급되어 어딘가에 복사된 access token 자체를 "즉시 무효" 목록에 올리지는 않습니다. 실제로 로그인 상태에서 access token을 뽑아 두고 로그아웃한 뒤 그 토큰을 그대로 Supabase REST API에 다시 보내보니 응답이 그대로 200으로 왔습니다(자연 만료 전).

우리 앱을 정상적으로 쓰는 경로(브라우저 쿠키를 통한 재요청)에서는 로그아웃 즉시 쿠키가 지워지므로 이 문제가 드러나지 않지만, 토큰 자체가 유출되는 경우(XSS, 로그 유출 등)에는 남은 유효 시간만큼 위험이 남습니다. 완화책으로 Supabase 프로젝트의 access token 만료 시간을 짧게 유지하는 것이 있으나, 완전히 막는 방법은 아니라 여기 그대로 남겨 적습니다.

---

## 카드5 — 5일 실사용 기록

**질문**: 오늘 계획한 할 일을, 실제로 몇 분 동안 실행했는가?
**지표 · 단위**: 하루 실행 시간 합계, 분(minutes) — `execution_records.actual_minutes`의 그날 합
**계산 규칙**: 그날 기록이 없으면 0분으로 봄(제외하지 않음). 같은 할 일을 하루에 여러 번 완료 처리해 기록이 중복되면 전부 더함(별도 중복 제거 없음). 소수점은 만들어지지 않음(분 단위 정수 입력). 반올림 없음. 주 시작 요일: 월요일(ISO). 시간대: Asia/Seoul.
**계획 규칙 변경**: 3일차 진입 전(2일차 기록 뒤, 3일차 기록 앞) 계획 하나의 우선순위 또는 예상 시간을 한 번 바꾸고, 그 이유와 바꾸기 전/후 지표를 아래에 남깁니다.

| 일차 | 날짜(Asia/Seoul) | 하루 합계(분) | 계획 규칙 변경 | 배운 점 |
|---|---|---|---|---|
| 1 | 2026-09-08 | 30 | 없음 | 로그인해야만 어제 넣어둔 계획이 보였다 |
| 2 | 2026-09-09 | 30 | 없음 | 하루 걸러도 기록이 안 섞이고 그대로 이어짐 |
| 3 | 2026-09-10 | 30 | "토익 단어 외우기" 계획 우선순위를 보통 → 높음으로 변경 | 우선순위를 높음으로 수정하니 강조하는 색으로 바껴 더 눈에 띔 |
| 4 | 2026-09-11 | 30 | 없음(3일차 규칙 유지) | 규칙을 바꾼 뒤에도 하루 합계 계산 방식은 그대로라 비교가 쉬움 |
| 5 | 2026-09-12 | 30 | 없음(3일차 규칙 유지) | 같은 할 일을 완료→되돌리기→재완료 하면 기록이 합쳐지지 않고 각각 남는다는 걸 실수로 알게 됨 |
